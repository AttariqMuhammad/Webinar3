@model IEnumerable<Webinar.Models.PenggunaModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NamaDepan)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NamaBelakang)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NamaDepan)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NamaBelakang)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PenggunaID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PenggunaID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PenggunaID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
